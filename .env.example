# Copy to .env to override. Docker Compose reads .env automatically.
# Host port for the control-plane Postgres. Default 5433 avoids the
# native PostgreSQL service that usually holds 5432.
AUTHZPLANE_PG_PORT=5433
